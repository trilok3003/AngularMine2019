import { ArrowKeyScrollDirective } from './arrow-key-scroll.directive';

describe('ArrowKeyScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrowKeyScrollDirective();
    expect(directive).toBeTruthy();
  });
});
