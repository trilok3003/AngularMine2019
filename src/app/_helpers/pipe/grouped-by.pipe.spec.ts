import { GroupedByPipe } from './grouped-by.pipe';

describe('GroupedByPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupedByPipe();
    expect(pipe).toBeTruthy();
  });
});
