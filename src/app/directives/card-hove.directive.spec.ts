import { CardHoveDirective } from './card-hove.directive';

describe('CardHoveDirective', () => {
  it('should create an instance', () => {
    const directive = new CardHoveDirective();
    expect(directive).toBeTruthy();
  });
});
