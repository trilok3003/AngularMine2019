import { UniqueInArrayPipe } from './unique-in-array.pipe';

describe('UniqueInArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueInArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
