import { CustomCurrency1Pipe } from './custom-currency1.pipe';

describe('CustomCurrency1Pipe', () => {
  it('create an instance', () => {
    const pipe = new CustomCurrency1Pipe();
    expect(pipe).toBeTruthy();
  });
});
