import { UniqueObjectInArrayPipe } from './unique-object-in-array.pipe';

describe('UniqueObjectInArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueObjectInArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
