import { DessendingSortPipe } from './dessending-sort.pipe';

describe('DessendingSortPipe', () => {
  it('create an instance', () => {
    const pipe = new DessendingSortPipe();
    expect(pipe).toBeTruthy();
  });
});
