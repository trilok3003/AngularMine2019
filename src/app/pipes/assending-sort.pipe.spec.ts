import { AssendingSortPipe } from './assending-sort.pipe';

describe('AssendingSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AssendingSortPipe();
    expect(pipe).toBeTruthy();
  });
});
