import { FilterBykeyPipe } from './filter-bykey.pipe';

describe('FilterBykeyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBykeyPipe();
    expect(pipe).toBeTruthy();
  });
});
